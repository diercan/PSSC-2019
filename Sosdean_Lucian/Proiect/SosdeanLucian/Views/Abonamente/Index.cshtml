@model IEnumerable<SosdeanLucian.Models.Abonament>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Client.NumeClient)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Oferta.NumeOferta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Client.NumeClient)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Oferta.NumeOferta)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AbonamentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AbonamentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AbonamentID })
        </td>
    </tr>
}

</table>
