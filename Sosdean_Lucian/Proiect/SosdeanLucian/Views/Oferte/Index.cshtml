@model IEnumerable<SosdeanLucian.Models.Oferta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NumeOferta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pret)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NumeOferta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pret)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OfertaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OfertaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OfertaID })
        </td>
    </tr>
}

</table>
