@model IEnumerable<SkinShopCSGO.Models.Skin>


<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Condition)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Collection)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Condition)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Collection)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SkinId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SkinId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SkinId })
        </td>
    </tr>
}

</table>
