namespace Web.Models.DDD{
    public interface IAggregateRoot{
        //Empty interface created just to specifically show
        //what entity from the aggregate represent the aggregate root
    }
}